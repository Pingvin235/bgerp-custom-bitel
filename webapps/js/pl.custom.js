/*
 * Plugin Custom
 */
"use strict";

$$.custom = new function() {
	const debug = $$.debug("custom");
	
	this.license = new function() {
		const calc = (processIds) => {
			
		}

		// public functions
		this.calc = calc;
	}
}
